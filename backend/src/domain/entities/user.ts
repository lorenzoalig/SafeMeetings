import { Credentials } from "../valueobjects/credentials";

class User {
    constructor(
        id: number,
        credentials: Credentials,
        level: number,
        profile_img: String
    ) {}
}